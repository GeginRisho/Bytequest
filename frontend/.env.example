# ByteQuest Frontend environment configuration template
# The Vite app resolves the backend host dynamically from the browser domain,
# but you can define a custom API target here if running in an isolated container.

VITE_API_URL=http://localhost:5000
VITE_SOCKET_URL=http://localhost:5000
VITE_DEMO_MODE=false

